# 시작하기

## 문서 추가 방법

1. `docs/` 폴더 아래에 `.md` 파일 추가
2. `mkdocs.yml`의 `nav` 항목에 경로 추가
3. 커밋 후 푸시하면 자동 배포

## 로컬 미리보기

```bash
pip install mkdocs-material
mkdocs serve
```

`http://127.0.0.1:8000` 에서 확인 가능합니다.
